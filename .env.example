# Copy this file to .env and fill in your keys

# Server-side only (injected by the Cloudflare Pages Function / Vite proxy)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Server-side only (injected by the Cloudflare Pages Function / Vite proxy)
GEMINI_API_KEY=your-gemini-api-key
